services:
  poc-api:
    image: poc-api:latest
    pull_policy: if_not_present
    ports:
      - "5000:5000"
    environment:
      DB_HOST: db-poc.dev-alphact.internal
      DB_NAME: dbpoc
      DB_USER: 'heimdall-db'
      DB_PASSWORD: 'mh8BgLKInh/OZuUIZSqy6e2s'
      DB_DEBUG: false
      PATH_ASSET: public/uploads
      BASE_URL: http://127.0.0.1:5000
      BASE_URL_FE: http://127.0.0.1
      JWT_TOKEN: poc
      JWT_REFRESH_TOKEN: poc-refresh
      JWT_TOKEN_EXPIRED: 15780000
      JWT_REFRESH_TOKEN_EXPIRED: 31560000
      DEFAULT_PASSWORD: poc123
      MAIL_DEBUG: false
      MAIL_HOST: mail.com
      MAIL_USERNAME: username
      MAIL_PASSWORD: password
      MAIL_PORT: 578
      MAIL_SERVER: server
      TOKEN_TELEGRAM: 5987860245:AAEHu96VGr35DdZcqQzvlvKXOVl_nvPHHB8
      CHAT_ID_TELEGRAM: 843549776
    command: >
      sh -c "npm run db:migrate && npm start"